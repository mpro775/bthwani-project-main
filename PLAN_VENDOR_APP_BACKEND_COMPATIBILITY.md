# خطة إصلاح وتوافق Vendor-App مع الباك إند

> **تاريخ:** 2025-02-12  
> **الهدف:** تحقيق توافق كامل بين تطبيق التاجر (vendor-app) والباك إند (backend-nest)  
> **الملف التفصيلي:** [`vendor-app/ops/PLAN_VENDOR_BACKEND_COMPATIBILITY.md`](vendor-app/ops/PLAN_VENDOR_BACKEND_COMPATIBILITY.md)

---

## ملخص سريع

| # | المشكلة | الأولوية | الوقت |
|---|---------|----------|-------|
| 1 | تسجيل الدخول: phone vs email | 🔴 حرج | 1–2 س |
| 2 | مسارات Merchant: `/merchant/` → `/merchants/` | 🔴 حرج | 30 د |
| 3 | Vendor ID vs Merchant ID للمنتجات | 🔴 حرج | 2–3 س |
| 4 | استيراد axiosInstance في Support | 🟡 متوسط | 15 د |
| 5 | استجابة API الموحدة | 🟡 متوسط | 1 س |

---

## خطوات التنفيذ السريعة

### 1. الباك إند — تسجيل الدخول
- تحديث `vendor.controller.ts` و `vendor.service.ts` لاستخدام `phone` بدلاً من `email`،
- إنشاء `VendorLoginDto` مع التحقق من صحة المدخلات.

### 2. vendor-app — مسارات Merchant
- في `src/api/merchant.ts`: استبدال `merchant` بـ `merchants` في جميع المسارات.

### 3. الباك إند + vendor-app — Vendor/Merchant
- إضافة endpoint مثل `GET /merchants/vendor/:vendorId/products` في الباك إند،
- أو إضافة endpoint `GET /merchants/by-vendor/:vendorId` لجلب Merchant ثم استخدامه.

### 4. vendor-app — Support
- تصحيح الاستيراد في `support.ts`: `import axiosInstance from './axiosInstance'`.

### 5. التحقق
- اختبار تسجيل الدخول، جلب المنتجات، الطلبات، والدعم.

---

*لتفاصيل التنفيذ والكود المطلوب، راجع الملف التفصيلي في `vendor-app/ops/`*
