#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🧪 Running tests before commit..."

# تشغيل الاختبارات السريعة
npm run test:fast

# فحص التغطية الأساسية
npm run test:cov -- --watchAll=false --passWithNoTests

echo "✅ Pre-commit checks completed!"
