# خطة إكمال نظام المحفظة (Wallet System Completion Plan)

## 📋 نظرة عامة

هذا المستند يوضح ما ينقص في نظام المحفظة الحالي وخطة إكماله بشكل كامل. النظام يحتوي على Backend قوي مع 30+ عملية، لكن هناك فجوات في الربط بين Frontend والBackend، وبعض العمليات غير مكتملة.

---

## 🔍 التحليل الحالي

### ✅ ما هو موجود ويعمل

#### Backend (NestJS)
- ✅ جميع Controllers (WalletController + V2WalletController)
- ✅ WalletService مع جميع العمليات الأساسية
- ✅ Entities (WalletTransaction, WalletEvent)
- ✅ DTOs (CreateTransactionDto, WalletBalanceDto)
- ✅ Authentication & Authorization
- ✅ 30+ API Endpoints جاهزة

#### Frontend - تطبيق المستخدم (app-user)
- ✅ `WalletScreen.tsx` - عرض الرصيد والمعاملات
- ✅ `walletApi.ts` - جميع دوال API جاهزة
- ✅ عرض القسائم المتاحة

#### Frontend - تطبيق السائق (rider-app)
- ✅ `wallet.tsx` - عرض الرصيد الأساسي
- ✅ `withdraw/index.tsx` - واجهة بسيطة للسحب

#### Frontend - لوحة التحكم (admin-dashboard)
- ✅ `WalletManagementPage.tsx` - إدارة محافظ المستخدمين
- ✅ `WalletStatement.tsx` - كشف حساب السائقين

---

## ❌ ما ينقص

### 1. Backend - Endpoints ناقصة

#### إحصائيات وإدارة
```typescript
// ❌ غير موجودة - يجب إضافتها
GET /wallet/admin/users          // جلب جميع المستخدمين مع محافظهم
GET /wallet/admin/stats          // إحصائيات المحفظة الشاملة
GET /wallet/admin/transactions   // جميع المعاملات مع فلترة متقدمة
GET /wallet/admin/user/:id       // محفظة مستخدم محدد (للإدارة)
```

#### عمليات غير مكتملة
- ⚠️ الكوبونات (Coupons) - V2 Controller
  - `applyCoupon()` - TODO: يحتاج ربط مع Finance Module
  - `validateCoupon()` - TODO: يحتاج ربط مع Finance Module
  - `getMyCoupons()` - TODO: يحتاج ربط مع Finance Module
  
- ⚠️ الاشتراكات (Subscriptions) - V2 Controller
  - `subscribe()` - TODO: يحتاج ربط مع Content Module
  - `getMySubscriptions()` - TODO: يحتاج ربط مع Content Module
  - `cancelSubscription()` - TODO: يحتاج ربط مع Content Module

---

### 2. تطبيق المستخدم (app-user) - واجهات ناقصة

#### شاشات غير موجودة:
1. ❌ **شاشة الشحن (Topup)**
   - شحن المحفظة عبر كريمي
   - عرض طرق الشحن المتاحة
   - سجل عمليات الشحن
   - التحقق من عملية الشحن

2. ❌ **شاشة السحب (Withdrawal)**
   - طلب سحب من المحفظة
   - عرض طرق السحب المتاحة
   - قائمة طلبات السحب الخاصة بي
   - إلغاء طلب سحب

3. ❌ **شاشة التحويل (Transfer)**
   - تحويل رصيد لمستخدم آخر (برقم الهاتف)
   - سجل التحويلات (صادرة/واردة)
   - إضافة ملاحظات للتحويل

4. ❌ **شاشة دفع الفواتير (Pay Bills)**
   - دفع فواتير الكهرباء
   - دفع فواتير الماء
   - دفع فواتير الإنترنت
   - سجل الفواتير المدفوعة

5. ❌ **شاشة الاسترجاع (Refund)**
   - طلب استرجاع معاملة
   - عرض طلبات الاسترجاع
   - حالة طلبات الاسترجاع

6. ❌ **شاشة تفاصيل المعاملة**
   - عرض تفاصيل معاملة محددة
   - معلومات إضافية (meta data)
   - رقم مرجعي بنكي (إن وجد)

---

### 3. تطبيق السائق (rider-app) - تحسينات ناقصة

#### واجهات ناقصة:
1. ❌ **سجل المعاملات الكامل**
   - عرض جميع المعاملات مع pagination
   - فلترة حسب النوع (credit/debit)
   - فلترة حسب الحالة (pending/completed/failed)
   - فلترة حسب الفترة الزمنية

2. ❌ **تحسين واجهة السحب**
   - عرض طرق السحب المتاحة
   - نموذج محسّن لطلب السحب
   - قائمة بطلبات السحب السابقة
   - حالة كل طلب سحب

3. ❌ **إحصائيات مفصلة**
   - الأرباح حسب اليوم/الأسبوع/الشهر
   - رسوم بيانية للأرباح
   - تفاصيل المدفوعات المعلقة
   - تاريخ السحوبات

---

### 4. لوحة التحكم (admin-dashboard) - واجهات ناقصة

#### واجهات غير متصلة:
1. ❌ **ربط Endpoints**
   ```typescript
   // في admin-dashboard/src/api/wallet.ts
   getWalletUsers() // ❌ يُرجع array فارغ - Endpoint غير موجود
   getWalletStats() // ❌ يُرجع بيانات فارغة - Endpoint غير موجود
   ```

2. ❌ **واجهة إدارة طلبات السحب**
   - عرض جميع طلبات السحب
   - فلترة حسب الحالة (pending/approved/rejected)
   - فلترة حسب نوع المستخدم (User/Driver)
   - الموافقة على طلب سحب
   - رفض طلب سحب مع إضافة سبب
   - عرض تفاصيل كل طلب

3. ❌ **واجهة إدارة الحجز (Escrow)**
   - حجز مبلغ من محفظة مستخدم
   - إطلاق المبلغ المحجوز
   - إرجاع المبلغ المحجوز
   - عرض جميع المبالغ المحجوزة

4. ❌ **واجهة عرض المعاملات**
   - عرض جميع المعاملات مع فلترة متقدمة
   - فلترة حسب المستخدم
   - فلترة حسب النوع (credit/debit)
   - فلترة حسب الطريقة (kuraimi/transfer/etc)
   - فلترة حسب الحالة
   - فلترة حسب الفترة الزمنية
   - تصدير البيانات (CSV/Excel)

5. ❌ **لوحة إحصائيات وتقارير**
   - إحصائيات شاملة للمحفظة
   - إجمالي الأرصدة
   - إجمالي المعاملات
   - رسوم بيانية
   - تقارير مالية (يومية/أسبوعية/شهرية)

---

## 📅 خطة الإكمال

### المرحلة 1: Backend - Endpoints ناقصة (أسبوع 1)

#### الأولوية: عاجلة 🔴

**1.1 إضافة Admin Endpoints**
```typescript
// في wallet.controller.ts أو wallet-admin.controller.ts

@Get('admin/users')
@Roles('admin', 'superadmin')
async getWalletUsers(@Query() filters: WalletUsersFilterDto) {
  // جلب جميع المستخدمين مع محافظهم
  // فلترة حسب الرصيد، النوع، إلخ
}

@Get('admin/stats')
@Roles('admin', 'superadmin')
async getWalletStats(@Query() period?: string) {
  // إحصائيات شاملة:
  // - إجمالي المستخدمين
  // - إجمالي الأرصدة
  // - إجمالي المعاملات
  // - المعاملات اليوم
  // - متوسط الرصيد
}

@Get('admin/transactions')
@Roles('admin', 'superadmin')
async getAllTransactions(@Query() filters: TransactionFilterDto) {
  // جميع المعاملات مع فلترة متقدمة
}

@Get('admin/user/:id')
@Roles('admin', 'superadmin')
async getUserWallet(@Param('id') userId: string) {
  // محفظة مستخدم محدد
}
```

**1.2 إكمال عمليات الكوبونات والاشتراكات**
```typescript
// في v2-wallet.controller.ts
// ربط مع Finance Module للكوبونات
// ربط مع Content Module للاشتراكات
```

**الملفات المطلوبة:**
- `backend-nest/src/modules/wallet/wallet-admin.controller.ts` (جديد)
- `backend-nest/src/modules/wallet/dto/wallet-users-filter.dto.ts` (جديد)
- `backend-nest/src/modules/wallet/dto/transaction-filter.dto.ts` (جديد)
- تحديث `wallet.service.ts` لإضافة الدوال الجديدة

---

### المرحلة 2: لوحة التحكم - ربط وإكمال (أسبوع 2)

#### الأولوية: عاجلة 🔴

**2.1 ربط Endpoints الموجودة**
```typescript
// في admin-dashboard/src/api/wallet.ts
// تحديث getWalletUsers() و getWalletStats()
// لاستخدام الـ endpoints الجديدة
```

**2.2 واجهة إدارة طلبات السحب**
- إنشاء `WithdrawalManagementPage.tsx`
- عرض جميع طلبات السحب
- فلترة وترتيب
- الموافقة/الرفض
- عرض التفاصيل

**2.3 تحديث WalletManagementPage**
- ربط `getWalletUsers()` و `getWalletStats()`
- إضافة زر لعرض المعاملات
- إضافة زر لإدارة الحجز

**2.4 واجهة إدارة الحجز (Escrow)**
- إنشاء `EscrowManagementPage.tsx`
- حجز/إطلاق/إرجاع الأموال
- عرض المبالغ المحجوزة

**الملفات المطلوبة:**
- `admin-dashboard/src/pages/admin/wallet/WithdrawalManagementPage.tsx` (جديد)
- `admin-dashboard/src/pages/admin/wallet/EscrowManagementPage.tsx` (جديد)
- `admin-dashboard/src/pages/admin/wallet/TransactionsPage.tsx` (جديد)
- تحديث `admin-dashboard/src/api/wallet.ts`
- تحديث `admin-dashboard/src/pages/admin/WalletManagementPage.tsx`

---

### المرحلة 3: تطبيق المستخدم - واجهات جديدة (أسبوع 3-4)

#### الأولوية: مهمة 🟡

**3.1 شاشة الشحن (Topup)**
- إنشاء `TopupScreen.tsx`
- نموذج شحن عبر كريمي
- عرض طرق الشحن
- سجل عمليات الشحن
- التحقق من عملية الشحن

**3.2 شاشة السحب (Withdrawal)**
- إنشاء `WithdrawalScreen.tsx`
- نموذج طلب سحب
- عرض طرق السحب
- قائمة طلبات السحب
- إلغاء طلب سحب

**3.3 شاشة التحويل (Transfer)**
- إنشاء `TransferScreen.tsx`
- نموذج تحويل رصيد
- البحث عن مستلم برقم الهاتف
- سجل التحويلات
- تفاصيل التحويل

**3.4 شاشة دفع الفواتير (Pay Bills)**
- إنشاء `PayBillScreen.tsx`
- اختيار نوع الفاتورة
- إدخال رقم الفاتورة
- دفع الفاتورة
- سجل الفواتير المدفوعة

**3.5 شاشة الاسترجاع (Refund)**
- إنشاء `RefundRequestScreen.tsx`
- اختيار معاملة للاسترجاع
- إدخال سبب الاسترجاع
- عرض طلبات الاسترجاع

**3.6 شاشة تفاصيل المعاملة**
- إنشاء `TransactionDetailsScreen.tsx`
- عرض جميع تفاصيل المعاملة
- معلومات إضافية
- حالة المعاملة

**الملفات المطلوبة:**
- `app-user/src/screens/wallet/TopupScreen.tsx` (جديد)
- `app-user/src/screens/wallet/WithdrawalScreen.tsx` (جديد)
- `app-user/src/screens/wallet/TransferScreen.tsx` (جديد)
- `app-user/src/screens/wallet/PayBillScreen.tsx` (جديد)
- `app-user/src/screens/wallet/RefundRequestScreen.tsx` (جديد)
- `app-user/src/screens/wallet/TransactionDetailsScreen.tsx` (جديد)
- تحديث `app-user/src/navigation/WalletStack.tsx` (إضافة routes جديدة)

---

### المرحلة 4: تطبيق السائق - تحسينات (أسبوع 5)

#### الأولوية: مهمة 🟡

**4.1 سجل المعاملات الكامل**
- تحديث `wallet.tsx` أو إنشاء `TransactionsScreen.tsx`
- عرض جميع المعاملات
- فلترة وترتيب
- Pagination

**4.2 تحسين واجهة السحب**
- تحديث `withdraw/index.tsx`
- عرض طرق السحب المتاحة
- نموذج محسّن
- قائمة بطلبات السحب

**4.3 إحصائيات مفصلة**
- إضافة قسم إحصائيات في `wallet.tsx`
- الأرباح حسب الفترة
- رسوم بيانية (اختياري)

**الملفات المطلوبة:**
- `rider-app/app/(driver)/transactions.tsx` (جديد)
- تحديث `rider-app/app/(driver)/wallet.tsx`
- تحديث `rider-app/app/(driver)/withdraw/index.tsx`
- `rider-app/src/api/walletApi.ts` (جديد - إذا لم يكن موجود)

---

### المرحلة 5: لوحة التحكم - تقارير وإحصائيات (أسبوع 6)

#### الأولوية: متوسطة 🟢

**5.1 لوحة إحصائيات**
- إنشاء `WalletStatsDashboard.tsx`
- إحصائيات شاملة
- رسوم بيانية
- تقارير مالية

**5.2 واجهة عرض المعاملات**
- تحديث أو إنشاء `TransactionsPage.tsx`
- فلترة متقدمة
- تصدير البيانات

**الملفات المطلوبة:**
- `admin-dashboard/src/pages/admin/wallet/WalletStatsDashboard.tsx` (جديد)
- `admin-dashboard/src/pages/admin/wallet/TransactionsPage.tsx` (جديد أو تحديث)

---

### المرحلة 6: إكمال العمليات (أسبوع 7)

#### الأولوية: منخفضة ⚪

**6.1 إكمال الكوبونات**
- ربط `applyCoupon()` مع Finance Module
- ربط `validateCoupon()` مع Finance Module
- ربط `getMyCoupons()` مع Finance Module

**6.2 إكمال الاشتراكات**
- ربط `subscribe()` مع Content Module
- ربط `getMySubscriptions()` مع Content Module
- ربط `cancelSubscription()` مع Content Module

**الملفات المطلوبة:**
- تحديث `backend-nest/src/modules/wallet/v2-wallet.controller.ts`
- تحديث `backend-nest/src/modules/wallet/wallet.service.ts`

---

## 📊 جدول زمني مقترح

| المرحلة | المدة | الأولوية | الحالة |
|---------|-------|----------|--------|
| المرحلة 1: Backend Endpoints | أسبوع 1 | 🔴 عاجلة | ⏳ لم تبدأ |
| المرحلة 2: لوحة التحكم | أسبوع 2 | 🔴 عاجلة | ⏳ لم تبدأ |
| المرحلة 3: تطبيق المستخدم | أسبوع 3-4 | 🟡 مهمة | ⏳ لم تبدأ |
| المرحلة 4: تطبيق السائق | أسبوع 5 | 🟡 مهمة | ⏳ لم تبدأ |
| المرحلة 5: تقارير وإحصائيات | أسبوع 6 | 🟢 متوسطة | ⏳ لم تبدأ |
| المرحلة 6: إكمال العمليات | أسبوع 7 | ⚪ منخفضة | ⏳ لم تبدأ |

**المدة الإجمالية المقدرة: 7 أسابيع**

---

## 🎯 معايير الإكمال

### لكل مرحلة:
- ✅ جميع الملفات المطلوبة تم إنشاؤها
- ✅ جميع الـ Endpoints متصلة وتعمل
- ✅ الواجهات متجاوبة (Responsive)
- ✅ معالجة الأخطاء (Error Handling)
- ✅ Loading States
- ✅ اختبارات أساسية (إن أمكن)

### للكل:
- ✅ جميع العمليات متصلة بين Frontend و Backend
- ✅ جميع الواجهات المطلوبة موجودة
- ✅ تجربة مستخدم سلسة
- ✅ أداء جيد

---

## 📝 ملاحظات مهمة

1. **الأولوية**: ابدأ بالمرحلة 1 و 2 (Backend + Admin Dashboard) لأنها أساسية
2. **الاختبار**: اختبر كل endpoint بعد إضافته
3. **التوثيق**: وثّق أي تغييرات في API
4. **الأمان**: تأكد من أن جميع الـ Admin endpoints محمية بـ Roles Guard
5. **الأداء**: استخدم Pagination في جميع قوائم البيانات
6. **التجربة**: ركز على UX في الواجهات الجديدة

---

## 🔗 روابط مفيدة

- [Wallet Module README](./README.md)
- [Wallet API Documentation](../README.md)
- [Admin Dashboard API](../admin-dashboard/src/api/wallet.ts)
- [User App API](../../app-user/src/api/walletApi.ts)

---

## 📞 للاستفسارات

في حالة وجود أي استفسارات أو تعديلات على الخطة، يرجى مراجعة هذا المستند وتحديثه.

---

**آخر تحديث**: 2026-01-23
**الإصدار**: 1.0.0
